<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		    
<mapper namespace="com.goodee.market.trade.myPage.MyPageDAO">

	<!-- 판매리스트 -->
	<select id="getSellItemList" resultMap="getMyPageResult" parameterType="ItemDTO">	
<!-- 		SELECT I.ITEMTITLE, I.ITEMPRICE, M.MEMBERNICKNAME
			FROM ITEM I
	             INNER JOIN
	             ITEMLIKE_MEMBER IM
	             ON I.ITEMNUM= IM.ITEMNUM
	             
	             INNER JOIN
	             MEMBER M
	             ON M.MEMBERNUM= IM.MEMBERNUM
            WHERE M.MEMBERNUM=#{memberNum}; -->
            SELECT * FROM ITEM
		    WHERE ITEMNUM
		        =(SELECT ITEMNUM FROM SELLITEMLIST WHERE MEMBERNUM=#{memberNum})
	</select>
	
	
	<!-- 구매리스트 -->
	<select id="getBuyItemList" resultMap="getMyPageResult" parameterType="ItemDTO">
		SELECT * FROM ITEM
		    WHERE ITEMNUM
		        =(SELECT ITEMNUM FROM BUYITEMLIST WHERE MEMBERNUM=#{memberNum})
	</select>
	
	
	<!-- 후기리스트 -->
	<select id="getReviewList" resultMap="getMyPageResult" parameterType="ItemDTO">
		SELECT * FROM REVIEW
			WHERE MEMBERNUM = #{memberNum}
	</select>
	
	<resultMap type="ItemDTO" id="getMyPageResult">
		<id column="itemnum" property="itemNum"/>
				<result column="membernum" property="memberNum"/>
				<result column="itemtitle" property="itemTitle"/>
				<result column="itemcontents" property="itemContents"/>
				<result column="itemcategory" property="itemCategory"/>
				<result column="itemprice" property="itemPrice"/>
				<result column="itemduedate" property="itemDueDate"/>
				<result column="itemstatus" property="itemStatus"/>
				<collection property="itemImageDTOs" javaType="List" ofType="ItemImageDTO">
				<id column="filenum" property="fileNum"/>
				<result column="filename" property="fileName"/>
				<result column="oriname" property="oriName"/>
				</collection>
		</resultMap>
	
</mapper>	
